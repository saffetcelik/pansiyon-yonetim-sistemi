
## Build and Verification
- Whenever making code changes, ALWAYS run a local docker build (e.g. `docker compose build`) before considering the work complete, to ensure there are no compilation or runtime errors that would break the production deployment.
